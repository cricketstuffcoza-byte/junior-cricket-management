import { redirect } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
import { SignOutButton } from '../../dashboard/sign-out-button';
import { AvailabilityClient } from './availability-client';
export default async function ParentAvailabilityPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:items,error}=await supabase.rpc('jcm_parent_availability_feed');return <div className="shell"><header className="topbar"><div className="brand"><div className="brand-mark">J</div><div>Junior Cricket Management<small>Parent Portal</small></div></div><div style={{display:'flex',alignItems:'center',gap:14}}><span className="pill">PARENT</span><span style={{fontSize:13,color:'var(--muted)'}}>{user.email}</span><SignOutButton/></div></header><main className="main"><div className="eyebrow">Parent availability</div><h1>Upcoming fixtures</h1><p className="subtitle">Confirm whether your linked player is available. Coaches will use this information when selecting the squad.</p>{error?<div className="notice">Unable to load availability: {error.message}</div>:<AvailabilityClient items={items??[]}/>}</main></div>}
