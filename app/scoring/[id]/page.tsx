import {redirect} from 'next/navigation';
import {createClient} from '@/lib/supabase/server';
import {SignOutButton} from '../../dashboard/sign-out-button';
import {ScoringPageClient} from './scoring-page-client';
export default async function ScoringPage({params}:{params:Promise<{id:string}>}){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const id=(await params).id;const matchResult=await supabase.from('jcm_matches').select('id,team_a_id,team_b_id,age_group,ruleset,status,toss_winner_team_id,toss_decision,scheduled_at,result_winner_team_id,result_reason,result_margin').eq('id',id).single();const match=matchResult.data;if(!match)redirect('/operations');const [stateResult,inningsResult,matchPlayersResult,eventsResult,statsResult,teamsResult,homePlayersResult,awayPlayersResult]=await Promise.all([supabase.from('jcm_match_state').select('*').eq('match_id',id).single(),supabase.from('jcm_innings').select('*').eq('match_id',id).order('innings_no'),supabase.from('jcm_match_players').select('player_id,team_id,selected,batting_order,jcm_players(id,first_name,surname)').eq('match_id',id),supabase.from('jcm_scoring_events').select('id,sequence_number,event_type,runs,extras,dismissal_type,created_at').eq('match_id',id).order('sequence_number',{ascending:false}).limit(30),supabase.from('jcm_player_match_stats').select('*').eq('match_id',id),supabase.from('jcm_teams').select('id,name,school_id').in('id',[match.team_a_id,match.team_b_id]),supabase.from('jcm_team_players').select('player_id,team_id,status,jcm_players(id,first_name,surname)').eq('team_id',match.team_a_id).eq('status','ACTIVE'),supabase.from('jcm_team_players').select('player_id,team_id,status,jcm_players(id,first_name,surname)').eq('team_id',match.team_b_id).eq('status','ACTIVE')]);const state=stateResult.data;const innings=inningsResult.data??[];const matchPlayers=matchPlayersResult.data??[];const events=eventsResult.data??[];const stats=statsResult.data??[];const teams=teamsResult.data??[];if(!state)redirect('/operations');const players=(matchPlayers.filter((p:any)=>p.selected) as any[]).map((p)=>({id:p.player_id,team_id:p.team_id,batting_order:p.batting_order,name:p.jcm_players?`${p.jcm_players.first_name} ${p.jcm_players.surname}`:'Player'}));const selected=matchPlayers.filter((p:any)=>p.selected).map((p:any)=>({player_id:p.player_id,team_id:p.team_id,selected:p.selected}));const home=teams.find(t=>t.id===match.team_a_id);const away=teams.find(t=>t.id===match.team_b_id);if(!home||!away)redirect('/operations');return <div className="shell"><header className="topbar"><div className="brand"><div className="brand-mark">J</div><div>Junior Cricket Management<small>Universal Live Scoring</small></div></div><div style={{display:'flex',alignItems:'center',gap:14}}><span className="pill">{match.ruleset}</span><span style={{fontSize:13,color:'var(--muted)'}}>{user.email}</span><SignOutButton/></div></header><main className="main"><a href={`/operations/matches/${id}`} style={{color:'var(--sky-dark)',fontWeight:800,fontSize:13}}>← Match preparation</a><div className="banner" style={{marginTop:18}}><h2>{home.name} vs {away.name}</h2><p>{match.age_group} · {match.ruleset} · {match.status}</p></div><ScoringPageClient match={match} initialState={state} initialInnings={innings} players={players} initialEvents={events} initialStats={stats} home={home} away={away} homePlayers={(homePlayersResult.data??[]) as any} awayPlayers={(awayPlayersResult.data??[]) as any} selections={selected}/></main></div>}
