import {redirect} from 'next/navigation';
import {createClient} from '@/lib/supabase/server';
import {SignOutButton} from '../dashboard/sign-out-button';
import {MobileNav} from '../mobile-nav';
export const dynamic='force-dynamic';
export default async function ScorerPortal(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const{data:roles}=await supabase.from('jcm_user_roles').select('role').eq('user_id',user.id).eq('active',true);const ok=(roles??[]).some((r:any)=>r.role==='SCORER'||r.role==='SYSTEM_ADMIN');if(!ok)redirect('/dashboard');return <div className="shell"><header className="topbar"><div className="brand"><div className="brand-mark">J</div><div>Junior Cricket Management<small>Scorer Portal</small></div></div><div style={{display:'flex',alignItems:'center',gap:10}}><MobileNav isScorer={true} active="scorer"/><span style={{fontSize:13,color:'var(--muted)'}}>{user.email}</span><SignOutButton/></div></header><main className="main"><div className="banner"><h2>Scorer Portal</h2><p>Open prepared matches and continue authorised live scoring sessions.</p></div><div className="grid"><a className="card" href="/operations#matches"><div className="stat-label">Live Scoring</div><div className="stat">Matches</div><span className="pill">Open scoring</span></a><a className="card" href="/dashboard"><div className="stat-label">Workspace</div><div className="stat">Dashboard</div><span className="pill">JCM home</span></a></div></main></div>}
