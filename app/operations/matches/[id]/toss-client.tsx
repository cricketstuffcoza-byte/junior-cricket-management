'use client';
import {useState,useTransition} from 'react';import {saveToss} from '../actions';
type Team={id:string;name:string;age_group:string};type Match={id:string;team_a_id:string;team_b_id:string;toss_winner_team_id:string|null;toss_decision:string|null};
export function TossClient({match,home,away}:{match:Match;home:Team|null;away:Team|null}){const[pending,start]=useTransition();const[msg,setMsg]=useState('');const[winner,setWinner]=useState(match.toss_winner_team_id??'');const[decision,setDecision]=useState(match.toss_decision??'');return <section className="card section"><div className="section-title">Toss</div><p className="form-help">Record who won the toss and whether they chose to bat or bowl. This determines innings 1.</p><div className="field"><label>Toss winner</label><select value={winner} onChange={e=>setWinner(e.target.value)}><option value="">Select team…</option>{home&&<option value={home.id}>{home.name}</option>}{away&&<option value={away.id}>{away.name}</option>}</select></div><div className="field"><label>Decision</label><select value={decision} onChange={e=>setDecision(e.target.value)}><option value="">Select decision…</option><option value="BAT">Bat</option><option value="BOWL">Bowl</option></select></div><button className="button" disabled={pending||!winner||!decision} onClick={()=>start(async()=>{const fd=new FormData();fd.set('match_id',match.id);fd.set('toss_winner_team_id',winner);fd.set('toss_decision',decision);const r=await saveToss(fd);setMsg(r.message)})}>{pending?'Saving…':'Save toss'}</button>{msg&&<div className="success">{msg}</div>}</section>}
