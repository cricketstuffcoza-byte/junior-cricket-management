'use client';
import { useRef, useState, useTransition, type ReactNode } from 'react';
import { addCompetitionTeam, createCompetition, createFixture, publishFixture } from './actions';
type School={id:string;name:string}; type Season={id:string;school_id:string;name:string;year:number;status:string}; type Team={id:string;school_id:string;season_id:string;name:string;age_group:string;status:string}; type Competition={id:string;name:string;season_id:string|null;competition_type:string;status:string}; type Venue={id:string;name:string;school_id:string|null}; type Fixture={id:string;season_id:string|null;competition_id:string|null;home_team_id:string;away_team_id:string;scheduled_at:string;fixture_type:string;status:string;availability_deadline:string|null};
function Form({action,children,label='Save'}:{action:(fd:FormData)=>Promise<{ok:boolean;message:string}>;children:ReactNode;label?:string}){const[pending,start]=useTransition();const[msg,setMsg]=useState('');const ref=useRef<HTMLFormElement>(null);return <form ref={ref} className="admin-form" onSubmit={e=>{e.preventDefault();setMsg('');const fd=new FormData(e.currentTarget);start(async()=>{try{const r=await action(fd);setMsg(r.message);if(r.ok)ref.current?.reset()}catch(err){setMsg(err instanceof Error?err.message:'Unexpected error')}})}}>{children}<button className="button" disabled={pending}>{pending?'Saving…':label}</button>{msg&&<div className={msg.toLowerCase().includes('required')||msg.toLowerCase().includes('not authorised')||msg.toLowerCase().includes('invalid')?'error':'success'}>{msg}</div>}</form>}
export function OperationsClient({schools,seasons,teams,competitions,venues,fixtures}:{schools:School[];seasons:Season[];teams:Team[];competitions:Competition[];venues:Venue[];fixtures:Fixture[]}){const schoolName=(id:string)=>schools.find(x=>x.id===id)?.name??'Unknown school';const teamName=(id:string)=>teams.find(x=>x.id===id)?.name??'Unknown team';const seasonName=(id:string|null)=>{const s=seasons.find(x=>x.id===id);return s?`${s.name} ${s.year}`:'—'};const compName=(id:string|null)=>competitions.find(x=>x.id===id)?.name??'—';return <>
<div className="admin-grid">
<section className="card"><div className="section-title">Create competition</div><p className="form-help">Competitions are season-based. System Admin controls inter-school competitions and tournament structures.</p><Form action={createCompetition} label="Create competition"><div className="field"><label>Name</label><input name="name" placeholder="Junior Cricket League 2026" required/></div><div className="field"><label>Season</label><select name="season_id" required><option value="">Select season…</option>{seasons.map(s=><option key={s.id} value={s.id}>{schoolName(s.school_id)} — {s.name} {s.year}</option>)}</select></div><div className="field"><label>Type</label><select name="competition_type" defaultValue="LEAGUE"><option value="LEAGUE">League</option><option value="TOURNAMENT">Tournament</option><option value="FRIENDLY_SERIES">Friendly series</option><option value="OTHER">Other</option></select></div></Form></section>
<section className="card"><div className="section-title">Add team to competition</div><p className="form-help">Teams must belong to the same season as the competition and cannot be retired.</p><Form action={addCompetitionTeam} label="Add team"><div className="field"><label>Competition</label><select name="competition_id" required><option value="">Select competition…</option>{competitions.map(c=><option key={c.id} value={c.id}>{c.name} · {seasonName(c.season_id)}</option>)}</select></div><div className="field"><label>Team</label><select name="team_id" required><option value="">Select team…</option>{teams.filter(t=>t.status!=='RETIRED').map(t=><option key={t.id} value={t.id}>{t.name} · {t.age_group} · {schoolName(t.school_id)}</option>)}</select></div></Form></section>
<section className="card"><div className="section-title">Create fixture</div><p className="form-help">Create as draft, verify the schedule, then publish. Publishing automatically creates player availability requests.</p><Form action={createFixture} label="Create draft fixture"><div className="field"><label>Season</label><select name="season_id" required><option value="">Select season…</option>{seasons.map(s=><option key={s.id} value={s.id}>{schoolName(s.school_id)} — {s.name} {s.year}</option>)}</select></div><div className="field"><label>Fixture type</label><select name="fixture_type" defaultValue="FRIENDLY"><option value="INTERNAL">Internal school</option><option value="INTER_SCHOOL">Inter-school</option><option value="COMPETITION">Competition</option><option value="FRIENDLY">Friendly</option><option value="TOURNAMENT">Tournament</option></select></div><div className="field"><label>Home team</label><select name="home_team_id" required><option value="">Select home team…</option>{teams.filter(t=>t.status!=='RETIRED').map(t=><option key={t.id} value={t.id}>{t.name} · {t.age_group} · {schoolName(t.school_id)}</option>)}</select></div><div className="field"><label>Away team</label><select name="away_team_id" required><option value="">Select away team…</option>{teams.filter(t=>t.status!=='RETIRED').map(t=><option key={t.id} value={t.id}>{t.name} · {t.age_group} · {schoolName(t.school_id)}</option>)}</select></div><div className="field"><label>Competition (optional)</label><select name="competition_id" defaultValue=""><option value="">No competition</option>{competitions.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label>Venue</label><select name="venue_id" defaultValue=""><option value="">No venue selected</option>{venues.map(v=><option key={v.id} value={v.id}>{v.name}</option>)}</select></div><div className="field"><label>Date & time</label><input name="scheduled_at" type="datetime-local" required/></div><div className="field"><label>Availability deadline</label><input name="availability_deadline" type="datetime-local"/></div><div className="field"><label>Notes</label><input name="notes" placeholder="Optional notes"/></div></Form></section>
</div>
<section className="card section"><div className="section-title">Competitions <span className="count">{competitions.length}</span></div>{competitions.length===0?<div className="empty">No competitions configured yet.</div>:<div className="table-wrap"><table><thead><tr><th>Competition</th><th>Season</th><th>Type</th><th>Status</th></tr></thead><tbody>{competitions.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td>{seasonName(c.season_id)}</td><td>{c.competition_type.replaceAll('_',' ')}</td><td><span className="status">{c.status}</span></td></tr>)}</tbody></table></div>}</section>
<section className="card section"><div className="section-title">Fixtures <span className="count">{fixtures.length}</span></div>{fixtures.length===0?<div className="empty">No fixtures scheduled yet.</div>:<div className="table-wrap"><table><thead><tr><th>Fixture</th><th>Season</th><th>Type</th><th>Scheduled</th><th>Status</th><th/></tr></thead><tbody>{fixtures.map(f=><tr key={f.id}><td><strong>{teamName(f.home_team_id)} vs {teamName(f.away_team_id)}</strong><small>{compName(f.competition_id)}</small></td><td>{seasonName(f.season_id)}</td><td>{f.fixture_type.replaceAll('_',' ')}</td><td>{new Date(f.scheduled_at).toLocaleString('en-ZA')}</td><td><span className="status">{f.status}</span></td><td>{(f.status==='DRAFT'||f.status==='SCHEDULED')&&<Form action={publishFixture} label="Publish"><input type="hidden" name="fixture_id" value={f.id}/></Form>}{f.status==='PUBLISHED'&&<a className="secondary-button" href={`/operations/fixtures/${f.id}`}>Squads</a>}</td></tr>)}</tbody></table></div>}</section>
</>}
